{% set name = "pipreqs" %}
{% set version = "0.4.9" %}
{% set sha256 = "2dfa21631cb68a97515e222d6f0033b5bfb75823567ca2195d528efb18c97990" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  fn: {{ name }}-{{ version }}.tar.gz
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  noarch: python
  noarch: python
  noarch: python
  number: 0
  script: python setup.py install --single-version-externally-managed --record record.txt
  entry_points:
    - pipreqs=pipreqs.pipreqs:main

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - docopt
    - yarg

test:
  imports:
    - pipreqs

  commands:
    - pipreqs --help

about:
  home: https://github.com/bndr/pipreqs
  license: Apache-2.0
  license_family: Apache
  license_file: LICENSE
  summary: 'Pip requirements.txt generator based on imports in project'
  dev_url: https://github.com/bndr/pipreqs

extra:
  recipe-maintainers:
    - proinsias
    - ericmjls
